#!/usr/bin/env bash
## Note: this script should be running from
## the root of the repo
set -eu

script_dir=$(dirname "$0")

echo '--- check code formatting, such as yapf, was run prior to commit' 

yapf --diff --recursive  .
