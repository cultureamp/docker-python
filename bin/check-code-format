#!/usr/bin/env bash
## Note: this script should be running from
## the root of the repo
set -eu

script_dir=$(dirname "$0")

echo '--- check code formatting, e.g. yapf_reformat, is run prior to commit' 

$script_dir/python/yapf_reformat --diff --recursive .
